import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

// Virtual DOM  
// Real DOM

// Current State of the Real DOM
// Captured to the virtual DOM

// Change!

// Applies the changes to the virtual DOM
// Calculates most efficient
// Applies the changes to the real dom
